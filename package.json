{
  "name": "seo-optimizer-local",
  "version": "2.0.0",
  "description": "Sentinel — Enterprise SEO & Compliance Intelligence Platform. Runs locally on Mac Mini with SQLite.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "setup": "node server/setup-db.js",
    "seed": "node server/seed-demo.js",
    "reset": "rm -rf data && npm run setup && npm run seed"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1",
    "puppeteer": "^23.0.0",
    "cheerio": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
